/**
 * Under Creative Commons Licence
 * @author Simon Chauvin
 * @param radius
 * @returns {___that0}
 */
function fmCircle(x, y, radius) {
    "use strict";
    var that_ = {};

    /**
     * Position
     */
    that_.x = x;
    that_.y = y;

    /**
     * Radius
     */
    that_.radius = radius;

    return that_;
}